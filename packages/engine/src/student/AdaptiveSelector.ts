import type { StudentModel } from './StudentModel.js';

/**
 * Placeholder for future adaptive logic.
 */
export class AdaptiveSelector {
  constructor(studentModel: StudentModel) {
    // To be implemented
  }

  getTopicConfig(topicId: string) {
    return {
      targetQuestionCount: 3,
    };
  }
}
